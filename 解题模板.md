# C++ 解题模板

默认使用 C++17：

```cpp
#include <bits/stdc++.h>
using namespace std;
```

## 目录

1. [哈希表](#1哈希表)
2. [双指针](#2双指针)
3. [滑动窗口](#3滑动窗口)
4. [前缀和](#4前缀和)
5. [链表指针](#5链表指针)
6. [二叉树递归与分治](#6二叉树递归与分治)
7. [栈与单调栈](#7栈与单调栈)
8. [回溯](#8回溯)
9. [二分查找](#9二分查找)
10. [堆与优先队列](#10堆与优先队列)
11. [贪心](#11贪心)
12. [动态规划](#12动态规划)
13. [图论与 BFS / 拓扑排序](#13图论与-bfs--拓扑排序)
14. [复习检查清单](#14复习检查清单)

## 1、哈希表

**适用：** 需要快速判断元素是否出现、记录元素位置或统计频次的问题，例如两数之和、字母异位词分组、最长连续序列。

**模板代码：**

```cpp
unordered_map<int, int> mp;
for (int i = 0; i < nums.size(); ++i) {
    int need = target - nums[i];
    if (mp.count(need)) return {mp[need], i};
    mp[nums[i]] = i;
}
return {};
```

**例题及代码题解：**

### 例题 1：[1. 两数之和](https://leetcode.cn/problems/two-sum/)

遍历当前元素 `x` 时，目标是寻找 `target - x`。把已经遍历过的元素放入哈希表，就能把查找从 `O(n)` 降到 `O(1)`。

```cpp
vector<int> twoSum(vector<int>& nums, int target) {
    unordered_map<int, int> mp;
    for (int i = 0; i < nums.size(); ++i) {
        int need = target - nums[i];
        if (mp.count(need)) return {mp[need], i};
        mp[nums[i]] = i;
    }
    return {};
}
```

时间 `O(n)`，空间 `O(n)`。

### 例题 2：[49. 字母异位词分组](https://leetcode.cn/problems/group-anagrams/)

将每个单词排序后的结果作为哈希键，键相同的单词一定属于同一组。

```cpp
vector<vector<string>> groupAnagrams(vector<string>& strs) {
    unordered_map<string, vector<string>> mp;
    for (string word : strs) {
        string key = word;
        sort(key.begin(), key.end());
        mp[key].push_back(word);
    }
    vector<vector<string>> answer;
    for (auto& [key, words] : mp) answer.push_back(move(words));
    return answer;
}
```

时间约为 `O(n * k log k)`，`k` 为单词长度。

### 例题 3：[560. 和为 K 的子数组](https://leetcode.cn/problems/subarray-sum-equals-k/)

若当前前缀和为 `sum`，历史上出现过 `sum - k` 的位置都能形成合法子数组，因此哈希表记录的是前缀和出现次数。

```cpp
int subarraySum(vector<int>& nums, int k) {
    unordered_map<int, int> count{{0, 1}};
    int sum = 0, answer = 0;
    for (int x : nums) {
        sum += x;
        answer += count[sum - k];
        ++count[sum];
    }
    return answer;
}
```

时间 `O(n)`，空间 `O(n)`。

## 2、双指针

**适用：** 数组有序、需要从两端向中间搜索，或需要原地维护有效区间的问题，例如盛最多水的容器、三数之和、移动零。

**模板代码：**

```cpp
int left = 0, right = nums.size() - 1;
while (left < right) {
    updateAnswer(left, right);
    if (shouldMoveLeft()) ++left;
    else --right;
}
```

**例题及代码题解：**

### 例题 1：[11. 盛最多水的容器](https://leetcode.cn/problems/container-with-most-water/)

面积由较短的边决定。移动较长的一边不会增加短边高度，因此每次移动较短的一边。

```cpp
int maxArea(vector<int>& height) {
    int left = 0, right = height.size() - 1, answer = 0;
    while (left < right) {
        answer = max(answer, min(height[left], height[right]) * (right - left));
        if (height[left] < height[right]) ++left;
        else --right;
    }
    return answer;
}
```

时间 `O(n)`，空间 `O(1)`。

### 例题 2：[15. 三数之和](https://leetcode.cn/problems/3sum/)

#### 思路

目标是寻找所有 `a + b + c = 0` 的不重复三元组。直接枚举三个下标需要 `O(n^3)`，即使先固定一个数，剩下两个数仍可以通过双指针优化为 `O(n^2)`。

1. 先排序。排序后，固定 `nums[i]`，剩余区间 `[i + 1, n - 1]` 有序，可以用左右指针判断下一步向哪边移动。
2. 固定第一个数 `nums[i]`，令 `left = i + 1`、`right = n - 1`。
3. 计算三数之和：
   - `sum < 0`：和太小，需要增大它，因此 `left++`；
   - `sum > 0`：和太大，需要减小它，因此 `right--`；
   - `sum == 0`：记录答案，然后同时移动左右指针。
4. 去重是本题的重点：
   - 如果 `nums[i] == nums[i - 1]`，当前固定值产生的组合与上一次完全重复，直接跳过；
   - 找到答案后，跳过连续相同的 `nums[left]` 和 `nums[right]`，避免同一层重复记录；
   - 不能在进入下一轮时无条件跳过所有重复元素，否则可能错过同一固定值下的合法组合，去重应放在对应指针移动的位置。
5. 当 `nums[i] > 0` 时可以提前结束：由于数组已经排序，后面的数都不小于 `nums[i]`，三数之和不可能再等于 `0`。

#### 代码题解

```cpp
vector<vector<int>> threeSum(vector<int>& nums) {
    vector<vector<int>> answer;
    sort(nums.begin(), nums.end());

    const int n = static_cast<int>(nums.size());
    for (int i = 0; i < n - 2; ++i) {
        // 同一层固定值去重。
        if (i > 0 && nums[i] == nums[i - 1]) continue;

        // 排序后，固定值已经大于 0，后面不可能凑出 0。
        if (nums[i] > 0) break;

        int left = i + 1;
        int right = n - 1;
        while (left < right) {
            const long long sum = static_cast<long long>(nums[i])
                                + nums[left]
                                + nums[right];

            if (sum < 0) {
                ++left;
            } else if (sum > 0) {
                --right;
            } else {
                answer.push_back({nums[i], nums[left], nums[right]});
                ++left;
                --right;

                // 找到一个解后，跳过左右两侧的重复值。
                while (left < right && nums[left] == nums[left - 1]) ++left;
                while (left < right && nums[right] == nums[right + 1]) --right;
            }
        }
    }
    return answer;
}
```

例如 `[-1, 0, 1, 2, -1, -4]` 排序后为 `[-4, -1, -1, 0, 1, 2]`。固定 `-1` 时，左右指针可以找到 `[-1, -1, 2]` 和 `[-1, 0, 1]`；第二个 `-1` 会被固定值去重逻辑跳过。

时间复杂度为 `O(n^2)`：排序为 `O(n log n)`，外层枚举和内层双指针合计 `O(n^2)`；额外空间复杂度为 `O(1)`，不计算返回结果和排序实现可能使用的栈空间。

### 例题 3：[283. 移动零](https://leetcode.cn/problems/move-zeroes/)

#### 思路

题目要求将所有 `0` 移到数组末尾，同时保持非零元素的相对顺序，并且必须原地修改数组。因此核心是把问题拆成两个阶段：

1. 从左到右扫描数组，稳定地收集所有非零元素；
2. 扫描结束后，剩余位置自然由 `0` 填充。

可以使用快慢指针：

- `fast`：负责遍历每一个元素，不能漏掉任何位置；
- `slow`：指向下一个应该放置非零元素的位置，也就是当前有效非零区间的右边界。

扫描到 `nums[fast] == 0` 时，说明这个零暂时留在当前位置，`slow` 不动；扫描到非零元素时，将它交换到 `slow`，再让 `slow++`。

以 `[0, 1, 0, 3, 12]` 为例：

| 扫描到的元素 | `slow` 指向 | 操作 | 数组状态 |
| --- | ---: | --- | --- |
| `0` | `0` | 跳过，`slow` 不变 | `[0, 1, 0, 3, 12]` |
| `1` | `0` | 与 `nums[0]` 交换，`slow++` | `[1, 0, 0, 3, 12]` |
| `0` | `1` | 跳过，`slow` 不变 | `[1, 0, 0, 3, 12]` |
| `3` | `1` | 与 `nums[1]` 交换，`slow++` | `[1, 3, 0, 0, 12]` |
| `12` | `2` | 与 `nums[2]` 交换，`slow++` | `[1, 3, 12, 0, 0]` |

#### 循环不变量

在每次处理完 `fast` 后：

- `[0, slow)` 中全部是已经收集好的非零元素，并且顺序与原数组一致；
- `slow` 左侧的元素不再需要处理；
- `fast` 负责继续向右探索剩余元素。

因此，非零元素会按照出现顺序依次写入数组前部，零元素会被交换到后面。整个过程只使用两个下标，不需要额外数组。

#### 代码题解

```cpp
void moveZeroes(vector<int>& nums) {
    int slow = 0;
    for (int fast = 0; fast < static_cast<int>(nums.size()); ++fast) {
        if (nums[fast] == 0) continue;

        swap(nums[slow], nums[fast]);
        ++slow;
    }
}
```

当数组前面没有零时，`slow == fast`，交换只是自己与自己交换，不影响结果；也可以额外判断 `if (slow != fast)` 来减少无意义交换。

时间复杂度为 `O(n)`，每个元素最多被快指针访问一次；额外空间复杂度为 `O(1)`。由于非零元素按照原出现顺序写入，算法天然满足稳定性要求。

## 3、滑动窗口

**适用：** 连续子串或子数组，需要维护一个满足条件的动态区间，例如无重复字符、字母异位词匹配。

**模板代码：**

```cpp
int left = 0;
unordered_map<char, int> count;
for (int right = 0; right < s.size(); ++right) {
    ++count[s[right]];
    while (!valid(count)) --count[s[left++]];
    updateAnswer(left, right);
}
```

**例题及代码题解：**

### 例题 1：[3. 无重复字符的最长子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters/)

窗口扩张后，如果出现重复字符，就移动左端点直到窗口重新合法。

```cpp
int lengthOfLongestSubstring(string s) {
    vector<int> last(128, -1);
    int left = 0, answer = 0;
    for (int right = 0; right < s.size(); ++right) {
        left = max(left, last[s[right]] + 1);
        last[s[right]] = right;
        answer = max(answer, right - left + 1);
    }
    return answer;
}
```

时间 `O(n)`，空间 `O(|字符集|)`。

### 例题 2：[438. 找到字符串中所有字母异位词](https://leetcode.cn/problems/find-all-anagrams-in-a-string/)

窗口长度固定为 `p.size()`，维护窗口内 26 个字母频次，与 `p` 的频次相同时记录左端点。

### 例题 3：[239. 滑动窗口最大值](https://leetcode.cn/problems/sliding-window-maximum/)

使用单调递减队列保存下标：过期下标从队首删除，比当前值小的下标从队尾删除，队首就是最大值。

## 4、前缀和

**适用：** 区间求和、子数组和、需要把“连续区间”转化为两个前缀状态之差的问题。

**模板代码：**

```cpp
vector<int> prefix(nums.size() + 1, 0);
for (int i = 0; i < nums.size(); ++i) {
    prefix[i + 1] = prefix[i] + nums[i];
}
// nums[l..r] 的和：prefix[r + 1] - prefix[l]
```

**例题及代码题解：**

### 例题：[560. 和为 K 的子数组](https://leetcode.cn/problems/subarray-sum-equals-k/)

当前前缀和减去历史前缀和等于 `k`，就得到一个和为 `k` 的连续子数组。与第 1 类哈希模板结合后，复杂度为时间 `O(n)`、空间 `O(n)`。

```cpp
int subarraySum(vector<int>& nums, int k) {
    unordered_map<int, int> count{{0, 1}};
    int sum = 0, answer = 0;
    for (int x : nums) {
        sum += x;
        answer += count[sum - k];
        ++count[sum];
    }
    return answer;
}
```

## 5、链表指针

**适用：** 链表删除、合并、反转、找中点、检测环和重排。核心是明确每个指针的职责。

**模板代码：**

```cpp
ListNode dummy(0, head);
ListNode* prev = &dummy;
ListNode* cur = head;
while (cur) {
    if (shouldDelete(cur)) prev->next = cur->next;
    else prev = cur;
    cur = cur->next;
}
return dummy.next;
```

**例题及代码题解：**

### 例题 1：[203. 移除链表元素](https://leetcode.cn/problems/remove-linked-list-elements/)

你的公开题解中使用了 `first / pre / cur` 三个指针，并处理头部连续删除。C++ 中用虚拟头节点可以统一所有情况。

```cpp
ListNode* removeElements(ListNode* head, int val) {
    ListNode dummy(0, head);
    ListNode* prev = &dummy;
    ListNode* cur = head;
    while (cur) {
        if (cur->val == val) prev->next = cur->next;
        else prev = cur;
        cur = cur->next;
    }
    return dummy.next;
}
```

时间 `O(n)`，空间 `O(1)`。

### 例题 2：[206. 反转链表](https://leetcode.cn/problems/reverse-linked-list/)

每次保存下一个节点，再把当前节点指向前驱节点，最后返回新的头节点。

```cpp
ListNode* reverseList(ListNode* head) {
    ListNode* prev = nullptr;
    ListNode* cur = head;
    while (cur) {
        ListNode* next = cur->next;
        cur->next = prev;
        prev = cur;
        cur = next;
    }
    return prev;
}
```

### 例题 3：[141. 环形链表](https://leetcode.cn/problems/linked-list-cycle/)

快指针每次走两步，慢指针每次走一步；如果有环，两者最终会相遇。

### 例题 4：[142. 环形链表 II](https://leetcode.cn/problems/linked-list-cycle-ii/)

相遇后将一个指针放回头节点，两个指针每次走一步，再次相遇的位置就是环入口。

## 6、二叉树递归与分治

**适用：** 需要分别处理左右子树，再根据左右结果计算当前节点的问题，例如最大深度、翻转二叉树、验证性质和重建二叉树。

**模板代码：**

```cpp
int dfs(TreeNode* node) {
    if (!node) return baseValue;
    int left = dfs(node->left);
    int right = dfs(node->right);
    return combine(node, left, right);
}
```

**例题及代码题解：**

### 例题 1：[104. 二叉树的最大深度](https://leetcode.cn/problems/maximum-depth-of-binary-tree/)

空节点深度为 0，当前节点深度等于左右子树最大深度加 1。

```cpp
int maxDepth(TreeNode* root) {
    if (!root) return 0;
    return 1 + max(maxDepth(root->left), maxDepth(root->right));
}
```

### 例题 2：[105. 从前序与中序遍历序列构造二叉树](https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-inorder-traversal/)

你的公开题解已经采用“主函数递归 + `find_left_right` 辅助函数”的结构：前序第一个元素是根，在中序中找到根的位置后即可切分左右子树。

优化时用哈希表记录中序下标，避免每层线性查找。

### 例题 3：[226. 翻转二叉树](https://leetcode.cn/problems/invert-binary-tree/)

递归翻转左右子树，再交换当前节点的左右指针；也可以先交换再递归。

### 例题 4：[98. 验证二叉搜索树](https://leetcode.cn/problems/validate-binary-search-tree/)

不能只比较父子节点，需要维护当前节点允许的上下界，或利用 BST 中序遍历结果严格递增这一性质。

## 7、栈与单调栈

**适用：** 括号匹配、需要后进先出、寻找下一个更大/更小元素，以及维护单调性的区间问题。

**模板代码：**

```cpp
stack<char> st;
for (char c : s) {
    if (isLeft(c)) st.push(c);
    else {
        if (st.empty() || !match(st.top(), c)) return false;
        st.pop();
    }
}
return st.empty();
```

**例题及代码题解：**

### 例题 1：[20. 有效的括号](https://leetcode.cn/problems/valid-parentheses/)

左括号入栈，右括号必须与栈顶匹配。遍历结束后栈为空才说明所有括号都闭合。

```cpp
bool isValid(string s) {
    stack<char> st;
    unordered_map<char, char> match{{')', '('}, {']', '['}, {'}', '{'}};
    for (char c : s) {
        if (!match.count(c)) st.push(c);
        else if (st.empty() || st.top() != match[c]) return false;
        else st.pop();
    }
    return st.empty();
}
```

### 例题 2：[155. 最小栈](https://leetcode.cn/problems/min-stack/)

普通栈保存元素，辅助栈同步保存当前最小值，使 `getMin()` 达到 `O(1)`。

### 例题 3：[739. 每日温度](https://leetcode.cn/problems/daily-temperatures/)

维护一个温度单调递减的下标栈。当前温度更高时，不断弹出栈顶并计算等待天数。

## 8、回溯

**适用：** 组合、排列、子集、棋盘搜索，以及需要枚举所有可能并进行剪枝的问题。

**模板代码：**

```cpp
void backtrack(State& state) {
    if (isComplete(state)) {
        answer.push_back(path);
        return;
    }
    for (const auto& choice : choices(state)) {
        if (!valid(choice, state)) continue;
        path.push_back(choice);
        makeChoice(state, choice);
        backtrack(state);
        undoChoice(state, choice);
        path.pop_back();
    }
}
```

**例题及代码题解：**

### 例题 1：[46. 全排列](https://leetcode.cn/problems/permutations/)

你的账号笔记已经记录了标准框架。用 `used[i]` 表示数字是否已经在当前路径中。

```cpp
vector<vector<int>> answer;
vector<int> path;

void dfs(vector<int>& nums, vector<bool>& used) {
    if (path.size() == nums.size()) {
        answer.push_back(path);
        return;
    }
    for (int i = 0; i < nums.size(); ++i) {
        if (used[i]) continue;
        used[i] = true;
        path.push_back(nums[i]);
        dfs(nums, used);
        path.pop_back();
        used[i] = false;
    }
}
```

### 例题 2：[78. 子集](https://leetcode.cn/problems/subsets/)

每到一个递归节点都可以把当前路径加入答案，然后决定是否选择后续元素；与全排列相比，不需要 `used` 数组。

### 例题 3：[17. 电话号码的字母组合](https://leetcode.cn/problems/letter-combinations-of-a-phone-number/)

你的账号笔记中已有该题的回溯记录：每一层对应一个数字，每次选择该数字映射的一个字母。

### 例题 4：[22. 括号生成](https://leetcode.cn/problems/generate-parentheses/)

只有左括号数量小于 `n` 时才能放左括号，只有右括号数量小于左括号时才能放右括号，这两个条件就是剪枝规则。

## 9、二分查找

**适用：** 有序数组、旋转有序数组、答案具有单调性的最小值/最大值问题。

**模板代码：**

```cpp
int left = 0, right = nums.size() - 1;
while (left <= right) {
    int mid = left + (right - left) / 2;
    if (nums[mid] == target) return mid;
    if (nums[mid] < target) left = mid + 1;
    else right = mid - 1;
}
return -1;
```

**例题及代码题解：**

### 例题 1：[34. 在排序数组中查找元素的第一个和最后一个位置](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array/)

分别进行两次二分：第一次找第一个大于等于 `target` 的位置，第二次找最后一个小于等于 `target` 的位置。

```cpp
int lowerBound(vector<int>& nums, int target) {
    int left = 0, right = nums.size();
    while (left < right) {
        int mid = left + (right - left) / 2;
        if (nums[mid] < target) left = mid + 1;
        else right = mid;
    }
    return left;
}

vector<int> searchRange(vector<int>& nums, int target) {
    int left = lowerBound(nums, target);
    int right = lowerBound(nums, target + 1) - 1;
    if (left == nums.size() || nums[left] != target) return {-1, -1};
    return {left, right};
}
```

### 例题 2：[74. 搜索二维矩阵](https://leetcode.cn/problems/search-a-2d-matrix/)

你的账号笔记已经将矩阵视为一维有序数组。令 `mid` 映射为 `row = mid / n`、`col = mid % n`，即可直接二分。

### 例题 3：[704. 二分查找](https://leetcode.cn/problems/binary-search/)

你的账号笔记中记录了递归二分。建议优先掌握上面的迭代左闭右闭模板，减少递归边界错误。

## 10、堆与优先队列

**适用：** 维护当前最大/最小元素、Top K、动态中位数和多路归并。

**模板代码：**

```cpp
priority_queue<int> maxHeap;
priority_queue<int, vector<int>, greater<int>> minHeap;
```

**例题及代码题解：**

### 例题 1：[215. 数组中的第 K 个最大元素](https://leetcode.cn/problems/kth-largest-element-in-an-array/)

使用大小为 `k` 的小根堆，堆顶始终是当前 Top K 中最小的元素。遍历结束后堆顶就是第 K 大元素。

```cpp
int findKthLargest(vector<int>& nums, int k) {
    priority_queue<int, vector<int>, greater<int>> pq;
    for (int x : nums) {
        pq.push(x);
        if (pq.size() > k) pq.pop();
    }
    return pq.top();
}
```

### 例题 2：[347. 前 K 个高频元素](https://leetcode.cn/problems/top-k-frequent-elements/)

先用哈希表统计频次，再用小根堆保留频率最高的 K 个元素。

## 11、贪心

**适用：** 每一步选择局部最优，并且可以证明局部选择不会破坏全局最优的问题。

**模板代码：**

```cpp
for (const auto& item : items) {
    if (canChoose(item, state)) {
        choose(item, state);
    }
}
return state.answer;
```

**例题及代码题解：**

### 例题 1：[55. 跳跃游戏](https://leetcode.cn/problems/jump-game/)

维护当前位置之前能到达的最远位置。如果当前位置超过最远位置，则无法继续；否则更新最远位置。

```cpp
bool canJump(vector<int>& nums) {
    int farthest = 0;
    for (int i = 0; i < nums.size(); ++i) {
        if (i > farthest) return false;
        farthest = max(farthest, i + nums[i]);
    }
    return true;
}
```

### 例题 2：[121. 买卖股票的最佳时机](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock/)

遍历时维护历史最低买入价，尝试用当前价格卖出，记录最大利润。

## 12、动态规划

**适用：** 存在重叠子问题和最优子结构，需要通过状态转移得到最优值、方案数或可行性的问题。

**模板代码：**

```text
1. 定义状态：dp[i] 或 dp[i][j] 表示什么
2. 初始化 base case
3. 推导状态转移方程
4. 确定遍历顺序
5. 确定最终答案位置
```

**例题及代码题解：**

### 例题 1：[70. 爬楼梯](https://leetcode.cn/problems/climbing-stairs/)

到第 `i` 阶只能从 `i-1` 或 `i-2` 阶到达，因此 `dp[i] = dp[i-1] + dp[i-2]`。

### 例题 2：[300. 最长递增子序列](https://leetcode.cn/problems/longest-increasing-subsequence/)

你的账号笔记已经强调了状态定义。`dp[i]` 表示以 `nums[i]` 结尾的最长递增子序列长度。

```cpp
int lengthOfLIS(vector<int>& nums) {
    vector<int> dp(nums.size(), 1);
    int answer = 0;
    for (int i = 0; i < nums.size(); ++i) {
        for (int j = 0; j < i; ++j) {
            if (nums[j] < nums[i]) dp[i] = max(dp[i], dp[j] + 1);
        }
        answer = max(answer, dp[i]);
    }
    return answer;
}
```

时间 `O(n^2)`，空间 `O(n)`。

### 例题 3：[416. 分割等和子集](https://leetcode.cn/problems/partition-equal-subset-sum/)

你的账号笔记已经将它归纳为 0-1 背包。目标是选出总和一半的子集；一维背包容量必须从后向前更新。

```cpp
bool canPartition(vector<int>& nums) {
    int sum = accumulate(nums.begin(), nums.end(), 0);
    if (sum % 2) return false;
    int target = sum / 2;
    vector<bool> dp(target + 1, false);
    dp[0] = true;
    for (int x : nums) {
        for (int j = target; j >= x; --j) {
            dp[j] = dp[j] || dp[j - x];
        }
    }
    return dp[target];
}
```

### 例题 4：[72. 编辑距离](https://leetcode.cn/problems/edit-distance/)

`dp[i][j]` 表示前 `i` 个字符和前 `j` 个字符的最少编辑次数。当前字符相同就继承左上角，否则在插入、删除、替换中取最小值加一。

## 13、图论与 BFS / 拓扑排序

**适用：** 网格连通块、最短层数、依赖关系、判断有向图是否有环。

**模板代码：**

```cpp
queue<int> q;
vector<int> indegree(n, 0);
for (auto& edge : edges) {
    graph[edge[0]].push_back(edge[1]);
    ++indegree[edge[1]];
}
for (int i = 0; i < n; ++i) {
    if (indegree[i] == 0) q.push(i);
}
int visited = 0;
while (!q.empty()) {
    int u = q.front();
    q.pop();
    ++visited;
    for (int v : graph[u]) {
        if (--indegree[v] == 0) q.push(v);
    }
}
return visited == n;
```

**例题及代码题解：**

### 例题 1：[207. 课程表](https://leetcode.cn/problems/course-schedule/)

课程是节点，先修关系是有向边。不断删除入度为零的课程；如果最后处理的课程数等于总课程数，说明没有环。

```cpp
bool canFinish(int numCourses, vector<vector<int>>& prerequisites) {
    vector<vector<int>> graph(numCourses);
    vector<int> indegree(numCourses, 0);
    for (auto& edge : prerequisites) {
        graph[edge[1]].push_back(edge[0]);
        ++indegree[edge[0]];
    }
    queue<int> q;
    for (int i = 0; i < numCourses; ++i) {
        if (indegree[i] == 0) q.push(i);
    }
    int visited = 0;
    while (!q.empty()) {
        int u = q.front();
        q.pop();
        ++visited;
        for (int v : graph[u]) {
            if (--indegree[v] == 0) q.push(v);
        }
    }
    return visited == numCourses;
}
```

### 例题 2：[994. 腐烂的橘子](https://leetcode.cn/problems/rotting-oranges/)

把所有初始腐烂的橘子同时加入队列，按层 BFS 扩散。每一层代表经过一分钟，最后检查是否仍有新鲜橘子。

## 14、复习检查清单

- 链表：是否需要虚拟头节点？删除节点后哪个指针移动？
- 二叉树：递归函数的返回值是什么？空节点返回什么？
- 回溯：终止条件、选择列表、剪枝、撤销是否完整？
- 二分：区间是 `[left, right]` 还是 `[left, right)`？
- 动态规划：状态、初始化、转移、遍历顺序、答案位置是否明确？
- C++：指针判空、`vector.size()` 的无符号类型、哈希默认插入和引用生命周期是否安全？
