# 账号题解与笔记归纳

## 一、账号中已有的公开题解

当前看到 5 篇公开题解：

1. 重建二叉树，python递归
2. 超过百分百用户！！！！我估计是 find_num < (list_len-1)/2 加速了。
3. python3，递归解法
4. python3，一个 first 指针，加上两个移动指针
5. python递归 / 普通解法

这些题解主要集中在二叉树和链表，说明你最早形成体系的方向是递归、链表指针和树的遍历。

## 二、从公开题解中提炼出的习惯

### 1. 喜欢用辅助函数拆分递归问题

在重建二叉树中，你把“根据根节点拆分左右子树的先序和中序序列”单独抽成 `find_left_right`，主函数只负责建立根节点并递归构建左右子树。

可复用原则：

```text
主函数：处理当前问题、建立当前节点
辅助函数：把输入转换成下一层递归需要的参数
递归：把同样的逻辑应用到子问题
```

### 2. 链表题倾向于显式维护多个指针

你的移除链表元素题解中使用了 `first`、`pre`、`cur`，并主动处理头部连续删除的情况。这说明你已经意识到链表题的核心不是遍历本身，而是“谁保存前驱、谁负责扫描、删除后谁移动”。

建议固定使用虚拟头节点，减少头节点特殊分支。

### 3. 会主动寻找减少无效遍历的方法

在链表中间节点题解中，你先计算长度，再只移动到中间位置，避免了无意义的后半段遍历。这个思路可以进一步升级为快慢指针：一次遍历同时完成定位。

### 4. 历史语言与后续目标

- 历史题解主要使用 Python / Python3。
- 后续目标语言改为 C++，因此模板会优先使用 STL 和 C++ 函数签名。
- 能接受递归，也会使用列表模拟栈。
- 代码注释较多，喜欢先解释变量职责，再写循环。
- 对“普通解法”和“优化解法”有区分意识。

迁移时重点对应关系：Python `list` → `vector` / `stack`，Python `dict` → `unordered_map`，Python `set` → `unordered_set`，Python `deque` → `deque`。

## 三、账号中的 17 条题目笔记反映出的知识结构

### 回溯

你已经记录了标准回溯框架：

```text
判断是否到达终止条件
    是：把当前路径加入答案
列出当前选择
做选择
递归
撤销选择
```

对应题目包括电话号码的字母组合、全排列、单词搜索等。

### 动态规划

你已经记录了三个关键点：

- 存在重叠子问题
- 具备最优子结构
- 最重要的是定义正确的状态和状态转移方程

在分割等和子集中，你已经把问题归纳为 0-1 背包；在最长递增子序列中，你也开始使用“已知前 k-1 个状态，推出第 k 个状态”的归纳思维。

### 二叉树遍历

你记录了中序、先序、后序遍历的迭代统一思路：使用栈模拟递归，并区分“入栈前操作”和“入栈后操作”。这是很有价值的模板化总结。

### 二分查找

你已经记录了“有序数组视为二分查找”的基本方向，也记录过递归写法。后续最需要补充的是区间定义：闭区间还是左闭右开，以及 `left`、`right` 的收缩时机。

### 链表与栈

你记录了反转链表的两种视角：栈 / 递归。你也记录了用两个栈实现队列，说明你在把抽象数据结构映射为基础容器方面比较自然。

### 原地技巧

在数组重复元素问题中，你已经比较过排序、集合、下标映射和原地标记，并注意到“是否允许修改原数组”会影响方案选择。这是很好的复杂度意识。

## 四、当前优势与需要补强的地方

### 优势

- 链表、树、回溯的递归直觉较强。
- 能够把题目转化为熟悉的数据结构模型。
- 已经开始关注时间复杂度、空间复杂度和遍历次数。
- 会记录同一道题的普通解法与优化解法。

### 需要补强

- 题解正文有几篇仍是“此处撰写解题思路”，需要补上为什么这样做、循环不变量和复杂度。
- 二分查找需要统一边界模板。
- 动态规划需要固定写出“状态定义、初始化、转移、遍历顺序、答案位置”。
- 链表需要更多虚拟头节点、快慢指针和反转区间题。
- 单调栈、单调队列、拓扑排序目前完成题较少，应结合已完成的滑动窗口最大值和课程表继续扩展。
